pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID = credentials('aws-access-key-id')
        AWS_SECRET_ACCESS_KEY = credentials('aws-secret-access-key')
        BACKEND_STORE_URI = 'sqlite:///mlflow.db'
        ARTIFACT_STORE_URI = 's3://bucketkadiri/Folder0/'
        MLFLOW_TRACKING_URI = 'http://mlflow-server:4000'
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Setup Docker Network') {
            steps {
                sh 'docker network rm mlflow-network || true && docker network create mlflow-network'
            }
        }

        stage('Generate .env File') {
            steps {
                dir('CICD') {
                    script {
                        writeFile file: '.env', text: """
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
BACKEND_STORE_URI=${BACKEND_STORE_URI}
ARTIFACT_STORE_URI=${ARTIFACT_STORE_URI}
MLFLOW_TRACKING_URI=${MLFLOW_TRACKING_URI}
"""
                        sh 'ls -la && cat .env' // Vérification en développement
    
